import React from 'react'; // ядро реакта
import ReactDOM from 'react-dom'; // монтируем компоненту в приложение
import App from './App';

ReactDOM.render(
    <App />,
  document.getElementById('root') // блок, который нужно вмонтировать
);
